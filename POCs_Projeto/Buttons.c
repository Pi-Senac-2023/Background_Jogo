#include "Buttons.h"
#include <allegro5/allegro5.h>

void freeAllSettings(buttonSettings* settings) {
	free(settings);
}

void createButton(buttonSettings *settings ,int widthWant,int heightWant) {

}

bool checkButtonOnSpace(bool status, int x, int y) {
	
}